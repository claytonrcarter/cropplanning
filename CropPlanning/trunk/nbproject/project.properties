application.args=
application.title=CropPlanning
application.vendor=Clayton
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/CropPlanning.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.Crop_Code-src_demo=../../My Documents/Software/Crop Code/src_demo
includes=**
jar.compress=false
javac.classpath=\
    ${reference.CPS-CropDB.jar}:\
    ${reference.CPS-CropPlans.jar}:\
    ${reference.CPS-CSV.jar}:\
    ${reference.CPS-Spec.jar}:\
    ${reference.CPS-HSQLDB.jar}:\
    ${reference.CPS-SwingUI.jar}:\
    ${reference.TableUtil.jar}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:unchecked -Xlint:deprecated
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.JUnit-PrivilegedAccessor.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.hsqldb.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.hsqldb.classpath=../../lib/hsqldb/lib/hsqldb.jar
# Property libs.javacsv.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.javacsv.classpath=../../../../lib/javacsv.jar
main.class=CPS.CropPlanning
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
project.CPS-CropDB=../../CPS-CropDB/trunk
project.CPS-CropPlans=../../CPS-CropPlans/trunk
project.CPS-CSV=../../CPS-CVS/trunk
project.CPS-HSQLDB=../../CPS-HSQLDB/trunk
project.CPS-Spec=../../CPS-Spec/trunk
project.CPS-SwingUI=../../CPS-SwingUI/trunk
project.TableUtil=../../TableUtil/trunk
reference.CPS-CropDB.jar=${project.CPS-CropDB}/dist/CPS-CropDB.jar
reference.CPS-CropPlans.jar=${project.CPS-CropPlans}/dist/CPS-CropPlans.jar
reference.CPS-CSV.jar=${project.CPS-CSV}/dist/CPS-CSV.jar
reference.CPS-HSQLDB.jar=${project.CPS-HSQLDB}/dist/CPS-HSQLDB.jar
reference.CPS-Spec.jar=${project.CPS-Spec}/dist/CPS-Spec.jar
reference.CPS-SwingUI.jar=${project.CPS-SwingUI}/dist/CPS-SwingUI.jar
reference.TableUtil.jar=${project.TableUtil}/dist/TableUtil.jar
run.classpath=\
    ${javac.classpath}:\
    ${reference.CPS-Spec.jar}:\
    ${build.classes.dir}:\
    ${libs.hsqldb.classpath}:\
    ${libs.javacsv.classpath}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.src.dir=src
test.test.dir=test
